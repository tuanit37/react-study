/*
 * DemoDataFile Messages
 *
 * This contains all the text for the DemoDataFile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DemoDataFile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DemoDataFile container!',
  },
});
