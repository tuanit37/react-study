import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Common.HlPlayerSmoo';

export default defineMessages({
  live: {
    id: `${scope}.live`,
    defaultMessage: 'Live streams',
  },
});
